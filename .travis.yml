language: java
jdk: oraclejdk8
install: true
script:
  - mvn clean package
  - docker build -t rso-projekt:customers-api-rso ./customers/
after_success:
  - docker login -u="$DOCKER_USERNAME" -p="$DOCKER_PASSWORD"
  - docker push rso-projekt:customers-api-rso
